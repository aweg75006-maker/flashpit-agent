# 复制为 .env 并填值。切勿提交 .env。
# 注意：值后面不要加行内注释（会被解析为值的一部分）

# ── LLM ──
# 更换服务商只改这里、不动代码：anthropic 走 Claude SDK；其余一律走 OpenAI 兼容 HTTP。
# provider: xiaomimimo | anthropic | openai | deepseek | qwen | 自建（任意 OpenAI 兼容服务）
LLM_PROVIDER=xiaomimimo
LLM_API_KEY=
# OpenAI 兼容服务商的端点/鉴权/思考开关（anthropic 不用这三项）：
#   LLM_BASE_URL    chat/completions 完整 URL；留空=小米 MiMo 默认端点
#   LLM_AUTH_STYLE  api-key（默认，MiMo）| bearer（OpenAI/DeepSeek/Qwen 等多数服务）
#   LLM_DISABLE_THINKING  true（默认，MiMo 推理模型须关思考保结构化输出）| false（非推理服务商置 false）
#     注：这只是**全局默认**。复杂任务（行程/深度调研/多步聚合）由编排层经 meta["thinking"]
#     动态开思考、提质（reasoning 留后端不下发）；结构化 JSON 的 Planner 恒不开。
# 例：切 DeepSeek → LLM_PROVIDER=deepseek, LLM_BASE_URL=https://api.deepseek.com/v1/chat/completions,
#     LLM_AUTH_STYLE=bearer, LLM_DISABLE_THINKING=false, LLM_MODEL_PRIMARY=deepseek-chat
LLM_BASE_URL=
LLM_AUTH_STYLE=api-key
LLM_DISABLE_THINKING=true
# MiMo docs: https://platform.xiaomimimo.com/docs/zh-CN/quick-start/first-api-call
LLM_MODEL_PRIMARY=mimo-v2.5-pro
LLM_MODEL_FALLBACK=mimo-v2.5
# 跨厂商备份档（provider[:model]）：active 厂商整链（含 429/上游抖动）耗尽后兜底一跳。
# 与 LLM_MODEL_FALLBACK（厂商内档位）是两层：同厂 fast=primary 时上游一抖整链即死，
# 2026-08-15 MiniMax 抖动实测即此。pinned 请求恒不跨（pin=不许漂移）；空=关。
# LLM_BACKUP=deepseek:deepseek-v4-flash
# 开放域/闲聊默认走"快"模型降延迟（task 4 模型分层）；deep 偏好时才用 primary
LLM_MODEL_FAST=mimo-v2.5
# T2 循环预算（M2 P2 分档）：下面两个是**全局覆盖**，留空才走分档
# （Interactive 2次/8s、Complex 3次/12s）。只在一键回退放宽前或评测钉档时才填。
# PLANNER_LOOP_MAX_ITERS=2
# PLANNER_LOOP_BUDGET_MS=5000
# PLANNER_LOOP_MAX_ITERS_COMPLEX=3     # Complex 档（adaptive：多意图/条件依赖链）
# PLANNER_LOOP_BUDGET_MS_COMPLEX=12000
# PLANNER_DEDUP_SIDE_EFFECTS=on        # 重复副作用防抖（replan 重复产出同一动作时不重发）
# VERIFY_OUTCOME=on                    # 执行后对账（capability.verification 声明式）
# VERIFY_MIRROR_STALE_S=180            # 云侧车况镜像陈旧上限（超时当"看不见"，不定罪）

# ── 多 LLM 源（HMI 设置页两级切换：厂商→模型，全局生效，默认 = 上面的 LLM_PROVIDER）──
# 每家填了 key 即在切换入口出现（未填置灰）。切换持久化到 Redis（llm:active）：网关重启/
# 重建自动恢复上次选择，不再回落 LLM_PROVIDER 默认（Redis 不可达时才降级内存态）。
# thinking/token 参数差异由网关按厂商内置（无需你配）。
# MiniMax（LLM MiniMax-M3 + TTS 同一把 key）。docs https://platform.minimaxi.com/docs
MINIMAX_API_KEY=
MINIMAX_LLM_MODEL=MiniMax-M3
# DeepSeek（deepseek-v4-pro / deepseek-v4-flash，OpenAI 兼容）。docs https://api-docs.deepseek.com
DEEPSEEK_API_KEY=
DEEPSEEK_MODEL_PRIMARY=deepseek-v4-pro
DEEPSEEK_MODEL_FAST=deepseek-v4-flash
# 阿里百炼 qwen3.7（复用现有百炼 key = 下面的 LLM_EMBED_API_KEY / DASHSCOPE_ASR_KEY，无需新 key）。
# 仅当百炼 LLM 用独立计费子账号时，才填 DASHSCOPE_LLM_KEY 覆盖。
QWEN_MODEL_PRIMARY=qwen3.7-max
QWEN_MODEL_FAST=qwen3.7-plus
# DASHSCOPE_LLM_KEY=

# ── Embedding（记忆语义检索）──
# memory 经 llm-gateway 取文本向量做语义召回。默认阿里云百炼 text-embedding-v4（1024 维）。
# MiMo 不提供 embeddings，故 embedding 用独立服务商+独立 key（与上面的 LLM_API_KEY 分开）。
# **只需填 LLM_EMBED_API_KEY=<你的百炼 key>**；其余有默认值。维度须与 EMBED_DIM 一致。
# 留空 LLM_EMBED_API_KEY → 无 embedding 源 → 记忆召回诚实降级 lexical（不影响其余功能）。
LLM_EMBED_API_KEY=
LLM_EMBED_URL=https://dashscope.aliyuncs.com/compatible-mode/v1/embeddings
LLM_EMBED_MODEL=text-embedding-v4
LLM_EMBED_AUTH_STYLE=bearer
LLM_EMBED_DIMENSIONS=1024
EMBED_DIM=1024
# 百炼 docs: https://help.aliyun.com/zh/model-studio/embedding-interfaces-compatible-with-openai

# ── ASR（语音识别，批处理面 /api/asr + gRPC Transcribe）──
# 引擎：auto（默认：LLM_PROVIDER 为 MiMo 系→MiMo；否则有百炼 key→桥接 dashscope 流式引擎；
#   都没有→mock）| mimo（钉住 MiMo，chat 换家后批处理仍用 MiMo）| dashscope | mock
# 即「不用 MiMo」时批处理 ASR 不会哑成 mock，跟随 dashscope 自动可用。
ASR_PROVIDER=auto
# MiMo ASR: mimo-v2.5-asr（复用 LLM_API_KEY——多 LLM 源惯例下该 env 即 MiMo 的 key）
# MiMo ASR docs: https://platform.xiaomimimo.com/docs/zh-CN/api/audio/Speech-Recognition
ASR_MODEL=mimo-v2.5-asr
ASR_LANGUAGE=zh
# MiMo 音频端点（批/流式 ASR/TTS 共用；与 chat 的 LLM_BASE_URL 独立），留空=官方集群
MIMO_AUDIO_BASE_URL=

# ── 流式 ASR（实时识别上屏）──
# 引擎：dashscope（DashScope 实时 qwen3，OpenAI Realtime 协议，真栈验证可用，默认）| mimo-chunked（MiMo 分块回退）| off
ASR_STREAM_PROVIDER=dashscope
# DashScope 实时 ASR（百炼）：留空则复用上面的 LLM_EMBED_API_KEY（同一把百炼 key）
DASHSCOPE_ASR_KEY=
DASHSCOPE_ASR_WS_URL=wss://dashscope.aliyuncs.com/api-ws/v1/realtime
# 默认模型（真栈实测可用，注意全小写 id）：qwen3-asr-flash-realtime-2026-02-10
ASR_STREAM_MODEL=qwen3-asr-flash-realtime-2026-02-10

# ── TTS（语音合成，批处理面 /api/tts + gRPC Synthesize；HMI 流式回退/唤醒提示音走这里）──
# 引擎：auto（默认：LLM_PROVIDER 为 MiMo 系→MiMo；否则桥接 TTS_STREAM_PROVIDER 对应流式
#   引擎；都不可用→mock）| mimo | cosyvoice | qwen | minimax | mock
# 即「不用 MiMo」时批处理 TTS（含唤醒提示音）自动跟随流式引擎，不再哑成 mock。
TTS_PROVIDER=auto
# MiMo TTS: mimo-v2.5-tts（复用 LLM_API_KEY）
# MiMo TTS docs: https://platform.xiaomimimo.com/docs/zh-CN/usage-guide/speech-synthesis-v2.5
TTS_MODEL=mimo-v2.5-tts
TTS_VOICE_ID=冰糖                 # 默认音色（中文女声）；非 MiMo 引擎收到不认识的音色会自动用该引擎默认
# 可选中文音色：茉莉(女) | 苏打(男) | 白桦(男)
# 可选英文音色：Mia(女) | Chloe(女) | Milo(男) | Dean(男)
# 默认：mimo_default（中国集群默认冰糖）
TTS_FORMAT=wav                    # 输出格式：wav（非流式）| pcm16（流式）

# ── 流式 TTS（服务端 PCM 流式合成 + barge-in）──
# 引擎：cosyvoice（DashScope cosyvoice-v3-flash，run-task，469ms 首帧，默认）| qwen（qwen3-tts-flash-realtime，含方言）
#   | mimo（MiMo v2.5 流式，复用上面 LLM_API_KEY）| minimax（MiniMax T2A 流式，复用 MINIMAX_API_KEY）| mock | off
# cosyvoice/qwen 复用 DASHSCOPE_ASR_KEY / LLM_EMBED_API_KEY（同一把百炼 key）；无 key → HMI 无感回退句级批处理 /api/tts
TTS_STREAM_PROVIDER=cosyvoice
# TTS_STREAM_MODEL 留空用引擎默认（cosyvoice-v3-flash / qwen3-tts-flash-realtime）
TTS_STREAM_MODEL=
# TTS_STREAM_VOICE 留空用引擎默认（cosyvoice: longxiaochun_v3 龙小淳 / qwen: Cherry）；HMI 设置可覆盖
TTS_STREAM_VOICE=
DASHSCOPE_TTS_INFERENCE_WS_URL=wss://dashscope.aliyuncs.com/api-ws/v1/inference
DASHSCOPE_TTS_REALTIME_WS_URL=wss://dashscope.aliyuncs.com/api-ws/v1/realtime
# MiniMax TTS（与 MiniMax LLM 同 MINIMAX_API_KEY）：模型 / 默认音色 / T2A 端点
# docs https://platform.minimaxi.com/docs/api-reference/speech-t2a-http.md
MINIMAX_TTS_MODEL=speech-2.8-turbo
MINIMAX_TTS_VOICE=female-tianmei
MINIMAX_T2A_URL=https://api.minimaxi.com/v1/t2a_v2

# ── 外部 API（选配，不配走 mock）──
# 注意：值后不要加行内注释；注释单独成行。
# 导航：高德 Web 服务。POI_VENDOR=amap 且 AMAP_KEY 非空 → 真实，否则 mock。
# docs: https://lbs.amap.com/api/webservice/guide/api/georegeo
POI_VENDOR=mock
AMAP_KEY=
# info-agent 用同一把 AMAP_KEY 将已授权定位坐标反查为可读地址；nearby-agent（周边发现）用它搜 POI 2.0 富数据（评分/人均/电话/营业时间）；不配置时仅显示“当前位置”/走 mock。
# 天气：和风 QWeather。显式设 WEATHER_VENDOR=qweather；完整 JWT 或 API Key 凭证也会自动启用真实 Provider。
# QWEATHER_HOST 填控制台的 API Host（如 xxxx.qweatherapi.com）或 devapi.qweather.com / api.qweather.com。
# 鉴权二选一（JWT 优先）：
#   (A) JWT（和风新版，推荐）：项目ID(sub) + 凭据ID(kid) + Ed25519 私钥
#       推荐 QWEATHER_PRIVATE_KEY 单行 PEM/裸 base64（换行用 \n）；文件路径用 QWEATHER_PRIVATE_KEY_PATH，容器内必须挂载该文件。
#   (B) API Key（旧版）：QWEATHER_KEY；仅可调用仍支持 API Key 的 V7 天气接口。
#       空气质量和天气预警使用 /airquality/v1/current/{latitude}/{longitude}、/weatheralert/v1/current/{latitude}/{longitude}，均必须使用上面的 JWT。
# docs: https://dev.qweather.com/docs/configuration/authentication/
WEATHER_VENDOR=mock
QWEATHER_HOST=devapi.qweather.com
QWEATHER_PROJECT_ID=
QWEATHER_KEY_ID=
QWEATHER_PRIVATE_KEY=
QWEATHER_PRIVATE_KEY_PATH=
QWEATHER_KEY=
# 联网搜索：优先 Exa（返回正文级内容，接地合成的原料前提）→ AnySearch → Bing → mock。
# Exa（AI 搜索，contents.text 直接回正文）：docs https://exa.ai/docs/reference/search
EXA_API_KEY=
EXA_BASE_URL=https://api.exa.ai
# AnySearch（兜底搜索 + extract 抓正文）：docs https://anysearch.com/docs
ANYSEARCH_API_KEY=
ANYSEARCH_BASE_URL=https://api.anysearch.com
# Bing Web Search（再降级）：docs https://learn.microsoft.com/en-us/bing/search-apis/bing-web-search/
BING_SEARCH_KEY=
# 新闻：SerpApi（Google News + Baidu News，AnySearch 兜底）→ mock。
# docs: https://serpapi.com/google-news-api | https://serpapi.com/baidu-news-api
SERPAPI_API_KEY=
# 赛事：api-football（实时比分/赛程，世界杯 league=1）。docs https://www.api-football.com/documentation-v3
API_FOOTBALL_KEY=
API_FOOTBALL_HOST=v3.football.api-sports.io
# 旧 NewsAPI（向后兼容，有 SERPAPI_API_KEY 时优先 SerpApi）
NEWS_API_KEY=
# 股票：Tushare（免费 API）→ mock。docs: https://tushare.pro/document/1?doc_id=5
TUSHARE_TOKEN=
# 旧 Alpha Vantage（向后兼容，有 TUSHARE_TOKEN 时优先 Tushare）
STOCK_API_KEY=
# 其他厂商（PoC 未接，预留）
# BAIDU_MAP_KEY=
# DIANPING_KEY=
# ETCP_KEY=
# PGVECTOR_DSN=

# ── 支付渠道（payment-gateway）──
# 渠道凭证只注入 payment-gateway 单服务（Agent 不持凭证红线）。
# PAYMENT_VENDOR：mock（默认）/ alipay / wechat / alipay,wechat。
# 显式配真实渠道但缺凭证 → 启动 fail-fast，不静默回退 mock。
PAYMENT_VENDOR=mock
PAYMENT_DEFAULT_CHANNEL=alipay
# 真渠道场景白名单（逗号分隔，如 parking.pay）。默认空=全部走 mock（fail-closed）：
# 防「PARKING_VENDOR=mock 算出的假金额走真渠道收真钱」——逐场景确认金额来源已
# 真实化后才加入。
PAYMENT_REAL_SCENES=
# 单笔上限（分），超限 Authorize 直接拒绝
PAYMENT_MAX_AMOUNT_FEN=20000
# 自有收单二维码有效期 / merchant_hosted 登记会话过期收口（秒）
PAYMENT_QR_EXPIRE_S=300
PAYMENT_MERCHANT_EXPIRE_S=1800
# mock 渠道模拟「用户扫码完成支付」的延迟秒：0=立即，-1=永不（测过期路径）
PAYMENT_MOCK_AUTOPAY_S=8
# merchant_hosted 支付链接域名白名单（逗号分隔，如 m.mcd.cn）；空=拒绝一切外部链接
PAYMENT_EXTERNAL_PAY_HOSTS=
# 支付宝当面付（真实联调推荐先走沙箱：open.alipay.com 控制台 → 开发工具-沙箱；
# 手机装「沙箱钱包」App 扫码。沙箱网关如下行；生产留空走默认 openapi.alipay.com）。
# 私钥/公钥支持裸 base64 或 \n 转义 PEM；文件形态用 *_PATH（容器内须挂载）。
ALIPAY_APP_ID=
ALIPAY_APP_PRIVATE_KEY=
# ALIPAY_APP_PRIVATE_KEY_PATH=
ALIPAY_PUBLIC_KEY=
# ALIPAY_PUBLIC_KEY_PATH=
# ALIPAY_GATEWAY=https://openapi-sandbox.dl.alipaydev.com/gateway.do
ALIPAY_GATEWAY=
# 微信支付 APIv3（无公开沙箱，需真实商户号；代码按 v3 真实实现+签名单测锁死）。
# 验签双模式：公钥模式（WECHATPAY_PUBLIC_KEY(_PATH)+_ID，2024 起新商户默认）优先；
# 未配则平台证书懒加载兼容。v1 无支付回调、纯轮询查单（车机无公网入站）。
WECHATPAY_MCHID=
WECHATPAY_MCH_SERIAL=
WECHATPAY_MCH_PRIVATE_KEY=
# WECHATPAY_MCH_PRIVATE_KEY_PATH=
WECHATPAY_APIV3_KEY=
WECHATPAY_APP_ID=
WECHATPAY_PUBLIC_KEY=
# WECHATPAY_PUBLIC_KEY_PATH=
WECHATPAY_PUBLIC_KEY_ID=

# ── 商户 MCP 会话 token（mcp-bridge，§9.9 批 3）──
# **商户会话 token ≠ 支付渠道凭证**：token 只注入 mcp-bridge（下单/查单的身份），
# 支付渠道密钥只注入 payment-gateway——两类凭证互不越界。
# 缺 token 时对应商户 server **整台诚实缺席**（准入日志记 env_var_missing），不静默 401。
# 麦当劳：mcp.mcd.cn 平台登录 → 控制台激活申请（限流 600 req/min，仅中国大陆）
MCD_MCP_TOKEN=
# 瑞幸：open.lkcoffee.com 登录申请；token 绑账号会话**约 1 个月有效**，
# 过期表现=瑞幸能力缺席，续期=重新申请后更新本值并重启 mcp-bridge
LUCKIN_MCP_TOKEN=

# ── 数据真实性严格栈（治理 P2）──
# on = 任何 provider 决议落 mock 即启动失败（含 LLM/embedding/ASR/TTS），演示/验收前翻开自证全真；
# 默认 off：CI 与离线开发全 mock 照跑。此外「显式配了真实源但构造失败」不受此开关控制、恒 fail-fast。
REQUIRE_REAL_PROVIDERS=off
# 豁免域（逗号分隔）：parking=停车数据源（ETCP）未接真、knowledge=车书暂无真实实现。
# payment 是独立决议域且**不在豁免**（2026-08-11 支付真实化，§9.17）：严格栈下
# PAYMENT_VENDOR=mock 即启动失败。
# REQUIRE_REAL_EXEMPT=parking,knowledge

# ── 基础设施 ──
REDIS_URL=redis://redis:6379/0
NATS_URL=nats://nats:4222
# ⚠ 口令段是 PoC 默认值。compose 里各服务的 DSN 已改成 ${POSTGRES_PASSWORD:-cockpit}
# 派生，本行只服务宿主侧脚本——prod 档要与 POSTGRES_PASSWORD 一起改（强制表第 8 项校验二者一致）。
POSTGRES_DSN=postgresql://cockpit:cockpit@postgres:5432/cockpit

# ── 服务地址（容器内 DNS）──
REGISTRY_ADDR=registry:50051
LLM_GATEWAY_ADDR=llm-gateway:50052
MEMORY_ADDR=memory:50053
CLOUD_PLANNER_ADDR=cloud-planner:50054
CLOUD_GATEWAY_ADDR=cloud-gateway:8080

# ── 部署注意 ──
# 浏览器 getUserMedia 要求安全上下文（HTTPS 或 localhost）。
# 车机 webview / 局域网演示环境需配置 HTTPS 或通过 localhost 代理访问。
# AUDIO_HTTPS=true  # 未来支持，当前请通过 localhost 或反向代理 HTTPS

# ── 端侧 ──
EDGE_GATEWAY_PORT=8090
FAST_INTENT_THRESHOLD_HIGH=0.85
# `_LOW` 从 M5 P3 起真正有了消费方（此前声明了却零调用点）：端侧语义 NLU 的 θ_low。
FAST_INTENT_THRESHOLD_LOW=0.5
# 端侧语义 NLU（M5 P3）：off | shadow。**这里刻意不写活跃行**——M2 踩过
# 「.env.example 的活跃行压死代码里的新默认值」，而这一项的默认（shadow）就是安全挡位。
# shadow = 只算不用，把「模型判定 vs 规则判定」打进 route.cloud span；模型缺失即 disabled。
# EDGE_NLU_MODE=shadow
# EDGE_NLU_DIR=./models/nlu       # 覆盖模型目录（默认 <repo>/models/nlu）

# ── Agent 注册中心 ──
# 长期不健康自动剔除：连续探测失败达此值（5s/轮 ≈ 10 分钟）整体注销（内存 + PG），
# Agent 改名/下线后的残留注册不再永生刷告警；活 Agent 每 10s 重注册自动豁免。0=禁用。
REGISTRY_EVICT_FAIL_COUNT=120

# ── 会话鉴权（R3.1，默认关；量产/演示翻开即闭环）──
# 默认全关/空 → 逐字保持现状（无 token 也能连、user_id=u1、下游按 PERMISSIONS_FAIL_OPEN 兜底）。
# 翻开演示：AUTH_REQUIRED=true + 配好下面三组 token + PERMISSIONS_FAIL_OPEN=false；
# HMI 无 token 连接会被 401 拒绝。
AUTH_REQUIRED=false
# 层 1（HMI↔edge-gateway）静态 token 表：条目用 ; 分隔，每条 token:user_id:vehicle_id:scope-csv。
# scope-csv 直接注入 meta.granted_scopes；商户账号型能力只给已认证主用户，匿名默认不含 merchant.*。
AUTH_TOKENS=
# 例（已认证主用户 demo；token 请自行生成，绝不要照抄示例值到生产）：
# AUTH_TOKENS=replace-with-random-token:u1:v1:vehicle.control,media.control,navigation,food.ordering,location.read,navigation.control,network.external,payment.invoke,merchant.read,merchant.write
# 匿名回退用户（AUTH_REQUIRED=false 且无有效 token 时用）；去掉了硬编码 user_id="u1"。
AUTH_DEFAULT_USER_ID=u1
# E2E runner 的短租约身份门。生产默认关闭；secret 仅由 runner owner
# 临时注入进程环境，child 只拿签名 token。不要把真实值写入本文件或根 .env。
E2E_IDENTITY_ENABLED=false
E2E_IDENTITY_SECRET=
# E2E runner-only memory extraction capability. Default off; the runner reuses
# the identity lease's 32-byte secret. Never persist a real value here or in .env.
E2E_CAPABILITY_ENABLED=false
E2E_CAPABILITY_SECRET=
VEHICLE_ID=v1
# HMI 连接 WS 及调用 Memory 全量 ForgetUser 时携带的 token（须与 AUTH_TOKENS 里某条一致）；
# llm-gateway 会把删除目标绑定到该 token 的 user_id，body 不得指定其他用户。留空=不带 token，
# Memory 删除及其商户草稿/Planner 挂起态联动会 fail-closed；这不是全 privacy registry saga。
# 商户浏览器验收同样要求显式认证。
VITE_WS_TOKEN=
# The same token is sent as a Bearer credential to destructive memory APIs.
# Those endpoints always require a valid AUTH_TOKENS entry, even when the
# WebSocket compatibility switch AUTH_REQUIRED=false. The two ephemeral-state NATS frames
# additionally derive a domain-separated key from GRPC_TLS_KEY; missing
# /certs/server.key fails closed and never enables an unsigned responder.
# 商户浏览器验收时，这里与上方已认证主用户条目的随机 token 保持一致；不要提交真实值。
# 层 2（edge-orchestrator↔cloud-gateway）通道 token（Hello 携带）与云侧允许集（逗号分隔，须含前者）。
CLOUD_CHANNEL_TOKEN=
CLOUD_CHANNEL_TOKENS=
# 例（去掉行首 # 生效）：
# CLOUD_CHANNEL_TOKEN=demo-channel-v1
# CLOUD_CHANNEL_TOKENS=demo-channel-v1
# 权限兜底（R2.2）：请求无 granted_scopes 时——true/默认=PoC 全开（保持现状）；
# false=fail-closed（仅无权限 Agent 可达）。R3.1 闭环演示置 false。
PERMISSIONS_FAIL_OPEN=true

# ── 服务间 mTLS（R3.2，默认关；量产/演示翻开）──
# 全默认关 → gRPC insecure，逐字保持现状。翻开演示：先往仓库根 certs/ 放入共享 mesh 证书
# （ca.crt/server.crt/server.key，本地证书生成流程产出、gitignore），
# 再 GRPC_TLS=on 起全栈（证书由 compose 无条件挂到各 mesh 服务 /certs，关时代码不读、无害）。
# 单张共享 mesh 证书作 server+client 双身份；客户端校验名固定为 GRPC_TLS_SERVER_NAME
# （无视拨号地址，解决 agent 用动态容器 hostname 注册）。
GRPC_TLS=off
GRPC_TLS_SERVER_NAME=cockpit-mesh
# 容器内证书路径（compose 已设默认，一般无需改）：
# GRPC_TLS_CA=/certs/ca.crt
# GRPC_TLS_CERT=/certs/server.crt
# GRPC_TLS_KEY=/certs/server.key

# ── 输入拒识 / 路由澄清（R4.4，置信度三段式；全 fail-open，=off 一键回今天）──
# REJECT：hands-free 语音源（meta.input_source=voice_*）+ LLM 判非受话 → 静默丢弃、不落库不进画像；
#         显式输入（文本/push-to-talk/候选选择）永不被拒。默认 on（作用域已被 hands-free + input_source 双重限定）。
REJECT_NON_ADDRESSED=on
# CLARIFY：真路由歧义句出 intent_choice 卡问一句再执行（明确句绝不反问）。默认 on（2026-07-08 真栈 CDP 验收后翻）。
CLARIFY_ENABLED=on
# CLARIFY_FALLBACK_MIN：LLM 挂/两次解析失败降级到语义 top-1 时的分数门槛，低于此值诚实降级不硬执行首个能力。
CLARIFY_FALLBACK_MIN=0.5

# ── 可观测 ──
OBS_COLLECTOR_PORT=8092
# 仅本地开发/演示开启；非开发环境必须设为 false
DEBUG_VEHICLE_CONTROL=true
OTEL_EXPORTER_OTLP_ENDPOINT=
LOG_LEVEL=info
# T3.6：Prometheus/Grafana 仅在 `docker compose --profile observability up` 时启用；
# PoC 默认凭证，对外暴露必须改（同 Postgres 密码的既有惯例）。
GRAFANA_ADMIN_PASSWORD=admin
# badcase 排查（会话/轮次/日志贯通）：
# 内容级采集（用户原话/话术/plan/LLM 输入输出，全部经统一脱敏）。开发/演示 on；
# **量产必须 off**（off 后只留长度+哈希指纹，链路形状排查不受影响）。
OBS_CONTENT_CAPTURE=on
# collector SQLite 保留期（天），badcase 标记的轮次豁免不清理
OBS_RETENTION_DAYS=7
# 日志上报门槛：≥ 该级别恒发 obs.log；带 trace_id 的 INFO 也发（轮次详情页执行痕迹）
LOG_SHIP_LEVEL=WARNING

# ── 通讯韧性（comms hardening；全部可选，代码内置安全默认值，留空即用默认）──
# gRPC keepalive：空闲连接也周期 ping，容器/NAT 掐断后一个周期内探测并重连重解析 DNS，
# 根治"依赖重启换 IP 后需重启依赖方"导致的断连/无响应。客户端 ping 周期须 ≥ 服务端最小间隔。
# GRPC_KEEPALIVE_TIME_MS=20000        # 每 20s 主动 keepalive ping
# GRPC_KEEPALIVE_TIMEOUT_MS=10000     # ping 10s 内无 ack 判定连接死亡并重连
# GRPC_MIN_PING_INTERVAL_MS=10000     # 服务端容忍 client ping 的最小间隔
# GRPC_MAX_MESSAGE_BYTES=16777216     # 单消息上限（16MB）
# GRPC_MAX_CONCURRENT_RPCS=0          # 服务端并发 RPC 上限，0=不限
# GRPC_SHUTDOWN_GRACE_S=10            # 优雅停机排空在途 RPC 的宽限秒数
# 熔断（云端 Agent dispatch）：连续失败 N 次→快速失败，不再每次吃满超时；冷却后半开探测自愈。
# CIRCUIT_FAILURE_THRESHOLD=5
# CIRCUIT_RECOVERY_TIMEOUT_S=30
# LLM 上游 429 带 Retry-After 时最多等待重试同模型的秒数上限（更长直接失败让上层诚实降级）
# LLM_429_WAIT_CAP_S=2

# ── 记忆图谱（M2 后半）──
# MEMORY_WEIGHTING=on      # 偏好加权与衰减；off=逐字回加权前（weight 恒 0 → 召回用 confidence）
# PLANNER_EMOTION=on       # 会话级情绪标注（prompt-only 不进 tool schema）→ HMI 下一轮 TTS 语气
# MEMORY_SESSION_TTL_S=604800  # 会话轮次原文（sess:*）TTL；对话原文是个人数据，无 TTL=永久留存
#                              # （2026-07-26 验收补口；ForgetUser 全量删同时级联清会话原文）

# ── 任务账本 Task Ledger（M2 P0）──
# 后台长任务（现 deep-research 异步深调研）的持久账本：可查进度、可喊停、中断可诚实报告。
# 复用 POSTGRES_DSN（compose 已注入）；无 PG → 账本静默禁用，异步调研仍照跑但不承诺可停可问。
# LEDGER_ORPHAN_TTL_S=90         # active 态超此秒数无心跳即判 orphaned（≈9 个心跳的余量）
# RESEARCH_TASK_DEADLINE_S=900   # 后台深调研截止时长（超时由心跳截停并主动告知）
# RESEARCH_TASK_LLM_MAX=6        # LLM 调用次数上限（Background 守卫③）
# RESEARCH_TASK_EXT_MAX=40       # 外部检索次数上限

# ── 智能提醒（reminder Agent）──
# 调度轮询秒（到点触发精度）；业务时区（中文时间表达解析与展示）
REMINDER_POLL_S=5
REMINDER_TZ=Asia/Shanghai

# ── 场景编排（scene-orchestrator Agent）──
# 用户自建场景持久化复用 POSTGRES_DSN（compose 已注入；无 PG 内存降级、重启丢失）。
# SCENE_VERIFY_WAIT_S=4        # 激活后多久做执行对账（等动作到端 + 状态 diff 经 NATS 回来）
# SCENE_TRIGGER_POLL_S=30      # 时间触发轮询间隔（场景触发不需要 reminder 的 5s 精度）
# SCENE_TRIGGER_THROTTLE_S=1800  # 同一触发器节流窗（边沿触发之上再加一层防骚扰）
# SCENE_CATALOG_DIR=           # VAL 词表目录（镜像构建期已 COPY；本地跑按「镜像→仓库相对」回退）
# LLM_MODEL_SCENE=             # scene.create 编译模型覆盖（留空用 primary 档）

# ── 记忆抽取（memory）──
# 合成会话跳过 LLM 抽取巩固（不烧 token、不污染真实画像）：按 session_id 前缀识别，
# 逗号分隔。旧 memtest- 也是不可信客户端标记；
# 抽取链路 E2E 只能使用 runner 签发的短时 capability session。
MEMORY_EXTRACT_SKIP_PREFIXES=eval-,e2e-,ctxe2e-,central-,review-,nightly-,replay-,probe-,smoke-,memtest-

# M0b Skill 层（规划知识声明式注入）：full|canary|shadow|off（默认 full=注入；Full Migration 后
# 中央 base 不含领域知识，shadow/off 仅研究/debug 档会缺知识）
SKILLS_MODE=full
# Skill 检索双通道（2026-07-26 补全，契约见 skills/README.md；默认值即代码默认，调优项
# 保持注释登记——活跃行会把未来代码默认值压死）：
# SKILLS_RETRIEVAL=hybrid     # hybrid=词法∪语义预筛（paraphrase 召回 0/11→11/11，eval_skills
#                             #   阈值扫描拍板）；lexical=纯词法回退档。语义通道 fail-open：
#                             #   llm-gateway Embed 不可用即回词法，不堵规划
# SKILL_SEM_THRESHOLD=0.40    # 语义补位余弦阈值（0.40=召回 11/11 且零新增噪声；0.45 掉 9/11）
# SKILL_EMBED_TIMEOUT=1.0     # 语义通道单次 Embed 超时秒（超时回词法 + 30s 冷却）
# SKILL_BUDGET=2600           # 注入块字符预算（超预算 guide 被裁并记 !clipped，重启生效）
#                             # ⚠ policy 常驻 + guide 检索**共用这一个预算**：加一条 policy
#                             #   会挤掉最相关的 guide。
# SKILL_TOP_K=3               # guide 预筛条数（重启生效）
# SKILL_MIN_SCORE=10          # 词法命中阈值（一个关键词=10 分，重启生效）

# M5 P1 范例库（落域范例第三通道，最软层——只作 few-shot 不做硬路由；契约见
# skills/exemplars/README.md）：full=注入｜shadow=只检索记录（A/B 对照）｜off=关
EXEMPLARS_MODE=full
# 调优项同样只登记不激活（默认值只活在 exemplars.py 一处）：
# EXEMPLARS_RETRIEVAL=hybrid      # hybrid=词法∪语义补位｜lexical=纯词法（零网络）
# EXEMPLAR_LEX_THRESHOLD=0.34     # IDF 加权 Dice 下限（eval_exemplars --scan 拍板：166 例
#                                 #   探针下 0.30 多 1 hit 却多 3 miss、0.40 少 3 miss 却丢 15 hit）
# EXEMPLAR_SEM_THRESHOLD=0.65     # 语义补位余弦下限（同扫描：0.70→0.65 是 +8 hit/+2 miss、
#                                 #   0.65→0.62 是 +4/+4 不再划算。比 skill 的 0.40 高是因为
#                                 #   范例比的是「话术 vs 话术」同文体，余弦基线整体抬高）
# EXEMPLAR_EMBED_TIMEOUT=1.0      # 语义通道单次 Embed 超时秒（与 skill 共享 30s 冷却）
# EXEMPLAR_BUDGET=700             # 注入块字符预算（超预算记 !clipped，重启生效）
# EXEMPLAR_TOP_K=3                # 单轮注入条数上限，同域最多 1 条（重启生效）

# M1b 情感 TTS 能力面（母提案 §4.H；cosyvoice v3 指令化情感/语速，缺省空=零行为变化；
# 话术层按情绪标签动态选参的接线等 §4.D emotion 落地）
# TTS_INSTRUCT_DEFAULT=       # 如「用温柔的语气说」
# TTS_SPEED_DEFAULT=          # 语速 0.5~2.0（DashScope rate）

# M1a submit_plan 结构化规划输出：
# on|off（默认 on）。
# on=规划轮经原生 function calling 强制输出合法 Plan JSON，协议失败轮内回退既有 JSON 路径
# （最坏 2 次调用=现状重试上限）；off=JSON 纯文本回退档（对照/应急）。
PLANNER_TOOLCALL=on

# 掉出工具通道后是否再要一次工具通道（默认 on）。依据 findings §23/§24：
# 走成 toolcall 的比例是 **provider 属性**（MiniMax 45~48% vs DeepSeek 100%，p≈0.0002），
# 而掉档轮的代价是实的（MiniMax 内部 toolcall 91% vs salvage 50%，p≈0.036，集中在需要
# 模型自己填结构化字段的多阶段计划上）。代价＝掉档轮多一次 LLM 调用，仍在原有 2 次上限内。
# ⚠ 只作用于「模型能说话但没用工具」这一种掉档；协议异常/provider 不认 tools 那一种
# 永远退 JSON 档（弱 tool-calling 厂商的可用性保障），与本开关无关。
# off=A/B 对照与应急回退。注意保持注释态（同下方 M2 教训）。
# PLANNER_TOOLCALL_SALVAGE_RETRY=on

# B5 §3 重试策略消融（跑批诊断用，**生产不要设**）：逗号分隔的策略名，命中即整条关掉。
# 名单与语义见 orchestrator/cloud/retry_policy.py::RETRY_POLICIES 与方案附录 A。
# ⚠ 未知名字直接抛、不静默当「什么都没关」——拼错却照跑，读数会被读成「关了也没变化」。
# 例：PLANNER_RETRY_DISABLE=salvage_wire_accepted（等价于 PLANNER_TOOLCALL_SALVAGE_RETRY=off
# 在「不再重试工具通道」这件事上的表现，两者一致性有断言钉着）。注意保持注释态。
# PLANNER_RETRY_DISABLE=

# 数据飞轮 P0：planner 能力
# 目录渲染预算（字符）。默认 16000——旧默认 8000 在 16 agent 满栈下会把 navigation 等
# 4 个无 route_hints 的 agent 整域裁出 prompt（保护判据与领域重要性无关），且裁剪全静默。
# 现每轮裁剪统计随 cloud.planning span 出观测（catalog_chars/catalog_dropped）。
# 注意保持注释态：激活行会在 copy 成 .env 后压死未来代码默认值（M2 教训）。
# PLANNER_CATALOG_BUDGET_CHARS=16000

# ── 统一主动引擎（M3 P0）──
# 主动消息的全局治理器（服务 proactive/）：订 agent.proactive.request → 六道闸 + 合并窗口
# → 发既有 agent.proactive。**一键回退 = 下面这行设 false 或停 proactive 容器**——生产方
# 经 runtime/proactive.py 走 request/ack，没人 ack 就直发老主题，逐字回落到治理器上线前。
# PROACTIVE_GOVERNOR_ENABLED=true
# PROACTIVE_MERGE_WINDOW_MS=1500   # 同窗到达的多条合并成一条（critical 档窗口恒为 0）
# PROACTIVE_DEDUP_WINDOW_S=600     # 同 dedup_key 的去重窗（**跨生产方**生效）
# PROACTIVE_MAX_PER_HOUR=6         # 全局频控（critical/user_contract 豁免但计数）
# PROACTIVE_HIGH_LOAD_SPEED=80     # ≥ 此车速判为高驾驶负荷 → 建议/环境类延后（读不到车速=放行）
# PROACTIVE_QUIET_HOURS=           # 免打扰时段如 23:00-07:00；**默认空 = 该闸不启用**（§9-3 拍板）
# PROACTIVE_DEFER_TICK_S=5         # 延后队列复评间隔

# 低电量主动建议（charging-planner 生产方，M3 P0 DoD 场景的电量那一半）
# CHARGING_LOW_SOC=20              # 电量跌破此值的**变沿**触发一次
# CHARGING_LOW_SOC_THROTTLE_S=1800 # 生产侧节流（防读数在阈值附近抖动重复播报）

# ── 端到端语音 S2S（M4）──
# 全部注释掉=用代码内默认；**HMI 侧默认挡位是 classic**（三段式），S2S 需用户在设置页
# 显式开启——它把「唤醒到说完」这段原始音频上云，是隐私口径变化点（RFC §5.4）。
# 一键停用 = 下面 S2S_PROVIDER 设 off（HMI 探测到不可用即回落三段式，无需改前端）。
# S2S_PROVIDER=dashscope           # dashscope | mock（离线假 provider）| off
# ⚠️ 型号必须支持 tools：qwen3-omni-flash-realtime（无 .5）会**静默丢弃** tools，
#    车控请求会被口头答应而不执行（P0 探针 ★T 实测）→ 工厂已拒绝该型号，别绕过。
# S2S_MODEL=qwen3.5-omni-flash-realtime
# S2S_API_KEY=                     # 留空则复用 DASHSCOPE_ASR_KEY / LLM_EMBED_API_KEY
# S2S_WS_URL=wss://dashscope.aliyuncs.com/api-ws/v1/realtime
# S2S_VAD_SILENCE_MS=800           # server VAD 静音尾（夹紧 [300,2000]）
# S2S_VAD_THRESHOLD=0.2
# S2S_SESSION_MAX_TURNS=20         # 长会话累积到此轮数 → 主动重建 session + 摘要重注入
# turn 悬挂看门狗：turn 开了却迟迟不收束（下行背压/provider 静默/任何成因）→ 诚实收
# turn.end(error, provider_silent)，不让 HMI 干等。0=关。
# S2S_TURN_TIMEOUT_S=45
# 域灰度旋钮（RFC §6.2）：**收放这段描述的边界**即收放 S2S 的自答范围，不做运行时按
# intent 拦截（判定点必须在模型生成前，生成后拦截必然截断已播出的音频）。
# S2S_ESCALATE_DESC=
# S2S_PERSONA=

# ── 声纹多用户 S2S P4（M4）──
# 唤醒后首句判断「谁在说话」→ occupant_id → 记忆按乘员隔离。
# **HMI 侧默认关**（设置页「乘员与声纹」显式开启）；**声纹不作鉴权因子=红线**——
# occupant_id 只进记忆域，权限/确认/VAL 一字不看它。
# 一键停用 = VOICEPRINT_PROVIDER=off（识别恒回 primary，逐字回落到 P4 之前）。
# VOICEPRINT_PROVIDER=auto        # auto（有模型即 campplus）| mock（离线假向量，严格栈拒绝）| off
# 声纹模型（28MB）gitignore，放入 models/voiceprint/ 即启用（可自行下载 campplus_zh-cn_16k-common.onnx）；
# **拉不到不阻塞**：网关决议 voiceprint=disabled，HMI 自动隐藏入口。
# VOICEPRINT_MODEL_PATH=/app/models/voiceprint/campplus_zh-cn_16k-common.onnx
# VOICEPRINT_MIN_SPEECH_MS=1500   # 短于此不识别（提不出稳定声纹，硬提=随机向量乱撞模板）
# 下面三个阈值由声纹探针实测标定，改动前先复标：
# VOICEPRINT_THRESHOLD=0.45       # top1 余弦下限。2026-07-26 按**真人真麦**从 0.62 下调：
#   真人同人 0.52 / 异人 0.12，0.62 把同人一并卡掉（现象=谁说话都认成同一个）。
#   合成音色把这件事标反了——TTS 音色共享信道特征、异人高达 0.65，逼阈值上抬；
#   真人分离度远好于合成，阈值反而该下放。**代理数据标的常量要在真实分布上复标。**
# VOICEPRINT_MARGIN=0.05          # top1-top2 差距下限；**真正起作用的控制量是它不是 threshold**
# VOICEPRINT_MIN_CONSISTENCY=0.50 # 注册三段的自洽度下限（低于即拒绝建模板，宁可不建坏模板）
# VOICEPRINT_THREADS=1            # ONNX 推理线程

# ── 视觉入口 P4（M4）──
# 「那是什么」单帧图片问答。**HMI 侧默认关**（设置页「看一看」显式开启）；
# 端侧命中视觉触发词才抓一帧，未命中一帧都不采集——隐私门控在采集侧。
# **图像不进对话链**：只有 frame_id 进 proto，图像本体在网关内存里活 TTL 秒，不落盘不落 Redis。
# VISION_PROVIDER=qwen-vl          # llm_runtime 的独立视觉档（internal，不进 HMI 大脑切换列表）
# VISION_MODEL=                    # 留空用该档 primary=qwen3-vl-plus
# VISION_MODEL_FALLBACK=           # 留空用 qwen-vl-max
#   ⚠️ **不要指向聊天大脑**：P4b 探针实测 qwen3.7-max 对多模态 content 直接 400，
#      而档位解析对不认识的模型是**静默回落 primary**——指错了不会报错，只会看不见图。
# VISION_FRAME_TTL_S=120           # 帧存活秒数（「问当下」，两分钟前的画面已不是同一个东西）
# VISION_FRAME_MAX=16              # 帧库 LRU 上限
# VISION_FRAME_MAX_BYTES=4194304   # 单帧上限；超限直接拒绝而非静默截断（半张图会让模型答错）

# ── 部署形态闸 DEPLOY_PROFILE（B3）──
# 上面所有安全开关都是「默认关、演示翻开」的 PoC 形态（R3.1/R3.2 拍板设计）。缺的不是这些
# 开关，而是**第四种运行形态**：一个 prod 档，在其中任何 fail-open 配置都让服务**拒绝启动**，
# 而不是打一行 warning 继续跑。三档：
#   dev （默认，含本项未设）零校验，逐字保持现状——CI / 离线开发不受任何影响；
#   demo 软校验：不满足强制表时启动打一段聚合 warning，不阻断（对外演示前自查用）；
#   prod 硬校验：任一项不满足即以 exit 78（EX_CONFIG）退出，逐项打印「哪个键、当前值、
#        要求值、为什么」。**未知档位值不回落 dev，直接拒启**（DEPLOY_PROFILE=production
#        拼错却按零校验跑，正是这道闸要消灭的形态）。
# 强制表（唯一真相源是代码 runtime/profile.py::CHECKS，Go 网关侧子集在 gateway/deployprofile）：
#   1 AUTH_REQUIRED=true            2 PERMISSIONS_FAIL_OPEN=false   3 GRPC_TLS=on
#   4 AUTH_TOKENS 非空且非示例值    5 CLOUD_CHANNEL_TOKEN ∈ CLOUD_CHANNEL_TOKENS 且非示例值
#   6 OBS_CONTENT_CAPTURE=off       7 REQUIRE_REAL_PROVIDERS=on
#   8 POSTGRES_PASSWORD 非默认且 POSTGRES_DSN 不内嵌默认口令
#  11 DEBUG_VEHICLE_CONTROL=false  12 GRAFANA_ADMIN_PASSWORD 非默认
#   （第 9 项 LLM 凭证由第 7 项的既有严格闸联动，不重复造；第 10 项 S2S/声纹/视觉的
#     隐私默认挡位写在 HMI 源码里、运行期 env 无承载，由源码级断言测试守）
# DEPLOY_PROFILE=dev
# prod 档要覆盖的两个 PoC 默认口令（dev 不设即沿用 compose 缺省，行为逐字不变）：
# POSTGRES_PASSWORD=
#   ⚠ 覆盖它就要同时改上面那行 POSTGRES_DSN 里内嵌的口令——容器口令换了、连接串没换，
#     宿主侧脚本会拿旧口令连；强制表第 8 项同时校验这两处，就是为了挡这个不一致。
# GRAFANA_ADMIN_PASSWORD=

# ── Registry 静态 admission（B3 §2.4；缺省空=关闭，现状不变）──
# 现状：Register 接受任意调用方的 Manifest，同 agent_id 直接覆盖——任何能连上 registry 的
# 进程都能把 navigation 换成自己。本项非空时开启 token→agent_id 绑定：调用方须在 gRPC
# metadata 带 x-agent-token，且申报的 agent_id 在该 token 的允许集内，否则拒绝注册（记审计日志）。
# 格式：<token>:<agent_id>[|<agent_id>...] ，多条用 , 分隔。
# REGISTRY_ADMISSION_TOKENS=tok-nav:navigation,tok-plan:charging-planner|scene-orchestrator
# Agent 侧携带同一 token（SDK 注册时读取）；留空=不带，admission 关闭时无影响。
# AGENT_REGISTRY_TOKEN=
