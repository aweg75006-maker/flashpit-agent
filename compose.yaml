# 唯一的 Docker Compose 入口。根目录 .env 是唯一的运行时环境来源。
# included compose 仍以 deploy/ 为项目目录，确保其相对 build 路径保持不变；
# env_file 明确指定回仓库根目录，避免误读 deploy/.env。
include:
  - path: deploy/docker-compose.yaml
    project_directory: deploy
    env_file: .env
